// Re-export para back-compat: los datos viven en ./models (sin SDK) para que
// los client components importen desde "@/lib/agent/providers/models".
export { CURATED_MODELS, type CuratedModel } from "./models";
